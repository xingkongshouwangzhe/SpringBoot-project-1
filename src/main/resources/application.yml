#服务端口
server:
  port: 8081

#数据源配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///music?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456


#默认配置
mybatis:
  mapper-locations: classpath:Mappers/*xml

#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
